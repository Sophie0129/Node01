
    <h1>login 입니다</h1>
    <a href="/logout">logout 이동</a>
    <a href="/">index 이동</a>
    <hr>
    이름 : <%= n %> <!--n :해당하는 값을 출력하겠다-->
    <hr>
    <% const msg = "java script 문법 사용가능" %>
    <%= msg %>
    <br>
    <% const arr = [10,20,30,40]; %>
    <%= arr[0] %> <%= arr[1] %> <%= arr[2] %><br>
    <script>
        const name = "홍길동동";
        console.log("msg : "+ '<%=msg%>' )
    </script>
    <!--스크립트를 쓰면 스크립트 밖의 변수를 사용할 수 없다.
    스크립트에서 전역변수를 부를 수는 있다. 반대는 불가능
백 언어를 프론트로 내보낼때 그대로 쓰면 안되고 프론트언어로 번역을 해주어야함
그걸 서버가 해준다?-->
